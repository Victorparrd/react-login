import React from 'react';
import ReactDOM from 'react-dom/client'; // Cambio en la importación
import App from './App';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
);
